apiVersion: v1
kind: Service
metadata:
  name: buggy-app-frontend
  namespace: buggy-app-challenge
  labels:
    app.kubernetes.io/name: buggy-app-frontend
    app.kubernetes.io/component: frontend
  annotations:
    hint: |
      The frontend service exposes the frontend deployment on port 3000.
      It should select pods with the appropriate labels to route traffic correctly.
